# C4 — <system / context name>

> Owner: Architecture (`domain-model`). Simon Brown: one diagram = one level of abstraction; legend; consistent notation.
> Keep these current — a stale diagram scores against *Architectural soundness*.

## Level 1 — System Context
```mermaid
graph TD
  subgraph Legend
    L1[Person]:::person
    L2[System]:::system
    L3[External system]:::ext
  end
  user[User / Persona]:::person -->|uses| sys[The System]:::system
  sys -->|integrates| ext[External System]:::ext
  classDef person fill:#08427b,color:#fff;
  classDef system fill:#1168bd,color:#fff;
  classDef ext fill:#999,color:#fff;
```

## Level 2 — Containers
```mermaid
graph TD
  user[User]:::person --> web[Web / client app]:::container
  web -->|HTTPS/JSON| api[API / backend service]:::container
  api --> db[(Datastore)]:::store
  api --> worker[Async worker / queue consumer]:::container
  classDef person fill:#08427b,color:#fff;
  classDef container fill:#1168bd,color:#fff;
  classDef store fill:#2e7d32,color:#fff;
```

> Replace the placeholder containers with the project's real ones. Add Level 3 (Components) and Level 4 (Code)
> only when they earn their keep (YAGNI).
